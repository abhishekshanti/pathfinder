To run this code you will first need to install all the required packages used in this project,  which can be done by this command:
    -   `make install`

Now to run this code run this command:
    -   `make run` / `make`